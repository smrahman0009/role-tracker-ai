# Pipeline-wide defaults. Per-user settings live in users/*.yaml.
jobs:
  country: ca          # ISO country code — "ca" = Canada
  results_per_page: 20 # default fetch size per query (override via --limit)
